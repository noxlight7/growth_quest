# This package defines the Django project configuration.
#
# The presence of this file allows Python to treat the containing
# directory as a package. Django uses this package to locate settings
# and other configuration modules.